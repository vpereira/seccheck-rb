necessary tools: vagrant

run the following commands:

    vagrant box add chef/opensuse-13.1
    vagrant up
    ... (it takes sometime)...
    vagrant ssh
    .... (the shared folder is on /vagrant) ...
